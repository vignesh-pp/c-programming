#include<stdio.h>
#include<string.h> 
int main(){
	char str[100];
	int i,j;
	scanf("%s",str);
	int len=strlen(str);
	for(i=0;i<len;i++){
		for(j=0;j<len;j++)
		{
			if(i==j || i+j==len-1){
				printf("%c",str[i]);
			}
			else
				printf(" ");
			}
			printf("\n");
	}
	return 0;
}
