#include<stdio.h>
int main(){
	int n,r,ec=0,oc=0;
	scanf("%d",&n);
	
	while(n>0){
		r=n%10;
		if(r%2==0)
		ec++;
		else
		oc++;
		n=n/10;
	}
	printf("even count %d\n",ec);
	printf("odd count %d",oc);
	return 0;
}
